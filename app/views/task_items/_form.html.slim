= form_for [@task, @task.task_items.build] do |f|
  .form-group
    = f.text_field :content, placeholder: 'Task Step', class: 'form-control', required: true
  .actions = f.submit class: 'btn btn-primary'

